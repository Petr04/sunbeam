'use strict';

/**
 * new-picture-order controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::new-picture-order.new-picture-order');
