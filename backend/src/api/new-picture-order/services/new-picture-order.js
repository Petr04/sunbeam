'use strict';

/**
 * new-picture-order service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::new-picture-order.new-picture-order');
